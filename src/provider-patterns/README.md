

1. Create the context `context/index.ts`
2. Create the Provider `provider/ThemeProvider.tsx`
3. Wrap the component hierarchy with Provider
4. create a hook to make the context available
5. use the data from context wherever needed
