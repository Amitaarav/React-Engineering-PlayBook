
Accessing DOM element directly::
`useRef provides a mutable container whose value persists across renders without causing re-renders.`

- React stores this object and returns the same object on every render.